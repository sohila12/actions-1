name: My First Woekflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
        - name: Welcome message
          run: echo "Hello"

        - name: List files
          run: ls

        - name: Read file
          run: cat README.md
          
   


        

